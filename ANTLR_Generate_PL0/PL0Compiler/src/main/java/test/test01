PROGRAM add
CONST z := 1,WH := 2;
VAR x,y;
BEGIN
        x:=1;
    y:=2;
    WHILE x<5 DO x:=x+1;
    IF y>0 THEN y:=y-1;
    y:=y+x;
    WHILE x<>y DO y := 3*(x+y)/2;
    WHILE x >= 1
        DO
            BEGIN
                y := (x+y-1)*3-6/(-1+2);
                ;;
            END

END